import { CustomStyleDirective } from './custom-style.directive';zz

describe('CustomStyleDirective', () => {
  it('should create an instance', () => {
    const directive = new CustomStyleDirective();
    expect(directive).toBeTruthy();
  });
});
